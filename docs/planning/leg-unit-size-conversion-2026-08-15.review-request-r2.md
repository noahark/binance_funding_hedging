只读评审任务（第二轮）。不要修改仓库任何文件，不要实现代码。

对象：docs/planning/leg-unit-size-conversion-2026-08-15.opus5.md（**r2，已返工**）
仓库：/Users/ark/Desktop/ai code/funding_hedging，分支 main，提交 e501cb0
状态：设计稿，代码一行未改。

背景：r1 经你与另一 provider 双评审均判 REWORK。本稿是返工版，采纳 Human 提出的新口径。
r1 的两份评审结论在同目录：*.review-claude-glm-result.md 与 *.review-grok-result.md。

r2 相对 r1 的核心变化：
1. 口径改为「表内存一个倍数，派生每条腿的一对乘数（价格 x = 1/倍数、数量 y = 倍数），
   在开仓/平仓/平滑/展示全部路径统一施加」。取代 r1 的「标准量纲 + 唯一出口换算」。
2. r1 双评审的 19 项发现已逐条应答，见 §10 应答表，无一项未处置。
3. 删除 r1 两处夸大表述：「唯一出口」、「单位面值 vs 倍率是两种数据模型」。
4. 新增 §2.4 论证取整不可交给人工过滤。
5. 护栏改为两道：建表前缀校验（第一道，零假阳性）+ 价格比值兜底（容差放宽至 [0.5x, 2x]）。

请按仓库 AGENTS.md 的评审口径给出 ACCEPT 或 REWORK，逐条列出问题并附文件路径与行号。
以下六问必须逐一正面回答（即 r2 §11）：

【问题 1】§2.2 的 (x, y) 每腿乘数口径是否覆盖完全？
是否存在既非「数量」也非「价格」、但同样受倍数影响的第三类量（例如名义价值、保证金、
手续费口径）？§4 主张 USDT 名义价值是换算下的不变量、无需第三类乘数——请独立验算。

【问题 2】§3 是否仍有遗漏路径？
r1 漏了平仓、平滑、展示三条，均已补入。请独立搜索是否还有第四类路径在消费两腿数量或
价格。建议覆盖：借币链路、划转链路、任务卡日志、导出/报表。遗漏一处就可能造成单腿敞口
或误导操作。

【问题 3】§3.1 的 split_legs 落点是否正确？
拒发放在 service.py:3570、prepare_attempt（3614）之前是否足够？三处装配点
（service.py:3570 / live_hedge_executor.py:828 / tests/fakes.py:152）共用一个函数，
是否还有第四处装配点？

【问题 4】§2.4 的判断是否成立？
本稿主张取整不可交给 Human 主观过滤，理由是：合约腿的量由「个数 ÷ 倍数」得出，若不落
在合约步长格子上会被交易所拒，而现货腿并发提交大概率已成交 → 单腿裸多。请核对该失败
路径在当前代码中是否真实存在。

【问题 5】§5.2 容差 [0.5x, 2x] 是否过宽？
本稿论据是「对数量级错误的检出率与紧容差完全相同，故应最小化误拦」。若认为存在只有紧
容差才能抓到的真实错误类型，请举出具体情形。

【问题 6】§8.2 的两条反自证约束是否足够？
约束 A：断言写在发给交易所的原始参数上；约束 B：测试夹具不得共用同一个 1000，且两个
夹具价格必须不同。若仍存在「实现与断言共用同一错误假设」的残余路径，请指出。

另请留意：r2 §3.3 更正了 r1 评审中的一处引用（glm 写作 `_is_close_flat`，该名称在仓库
中不存在，真实为 `_verify_close_flat` service.py:2393，判定在 :2406）。若你发现 r2 自身
也有类似的引用错误，请一并指出——本稿的价值高度依赖行号与事实准确。

约束：只读；不要改任何文件；不要实现；结论写在你的回复里。
