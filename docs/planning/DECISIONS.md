# Decisions

Status: approved decision log, as of 2026-08-13

This file records approved product, business, financial, and technical decisions.

Draft decisions belong in `reports/agent-runs/<stage-id>/` until user approval.

## Decision Log

> Note: historical Harness routing/workflow decisions recorded below (including
> `DEC-2026-07-05-001` stage-branch mode and `DEC-2026-07-08-001` model routing)
> are not active runtime authority after Harness v2. Current behavior comes only
> from `AGENTS.md`, `agents/roles.md`, the active `status.json`, and the active
> dispatch. The rows are preserved as decision history; do not rewrite them.

| ID | Date | Decision | Owner | Source |
|---|---|---|---|---|
| DEC-2026-08-20-001 | 2026-08-20 | **Hedge order trading fee costing and BNB price freezing v1 (成交手续费冻价成本 V1) is accepted and in live use.** Four frozen columns on `hedge_open_leg` (`fee_bnb_qty`, `fee_bnb_price`, `fee_other_qty`, `fee_other_asset`) and three on `hedge_open_cycle_close_log` (`trading_fee_usdt`, `fee_bnb_qty`, `trading_fee_incomplete`). Realtime hook uses strict commit-first sequence (orders commit terminal state first, then fire 1x trade history GET; failure logs audit event and never touches terminal state). BNB price is frozen at order fill time (1m K-line close price in historical backfill; memory cache <=300s or public price in realtime). Position aggregation calculates USD-equivalent fees using `cumulative_quote_amt / cumulative_base_qty` vwap (avg_price forbidden); if any open leg is incomplete, returns None/None/True (fail-closed, displaying `—` rather than partial/fake values). Historical `close_log` immutable protection: backfill only populates `hedge_open_leg` and never rewrites historical `close_log` rows (Breakpoint 1). Live backfill: 268/269 candidate legs populated into production database. | User / GLM impl, Moonshot review-1, Opus 5 review-2 | `reports/agent-runs/2026-08-19-hedge-order-fee-cost-v1/10-design.md`, `status.json` |
| DEC-2026-08-15-001 | 2026-08-15 | **1000x multiplier-contract leg-quantity conversion is CLOSED — not doing.** Supersedes the "awaits explicit Human authorization" half of `DEC-2026-08-07-006`; the fail-closed blocking half of that decision stands and is now the **intended end state, not a stop-gap** — its scaffolding (`PAUSE_REASON_MULTIPLIER_CLOSE_UNSUPPORTED` constant/registration/copy, the `_allow_multiplier_open` test fixture, the three `SPOT_MATCH_MULTIPLIER` interception sites) **must not be removed as dead code**. The six multiplier symbols (BONK/FLOKI/LUNC/PEPE/SHIB/XEC) stay unhedgeable with zero exposure. Closed on cost/benefit, NOT because the design was wrong: after five design revisions and three rounds of three-way review, two of three reviewers confirmed the design itself sound, and all remaining blockers were change-list completeness and document self-contradiction. Measured (Binance public API, 2026-08-15): hedgeable universe 358 → 364 (+1.7%); 11-month marginal contribution **1.72%**, of which **74% sits in `1000XEC` alone** and `1000SHIB` never crossed the 0.05% threshold in 500 settlement periods. Human's framing for any restart: "not touching XEC now does not mean never" — a restart should be justified by capability availability, not current revenue. **The sealed change lists are known to be incomplete** (at least 7 display cells plus the `residual` calculation path at `service.py:385`/`:1505`); restart guidance and the five-round review lessons are in `docs/planning/leg-unit-size-conversion-2026-08-15.CLOSED-lessons.md`. | User | `PROJECT_STATE.md` Live Risks / Open Follow-ups, `docs/product/PRD.md` §2.2 |
| DEC-2026-08-13-002 | 2026-08-13 | **Harness identity is named by model, never by terminal label.** Antigravity is a CLI wrapper, not a provider: the provider table entry becomes `gemini-3.1-pro` (the `agy` window) → `google`, and a session in that window running another vendor's model keeps that vendor's provider identity for isolation purposes. The same naming rule now applies to every Bookkeeper selection, so `status.json.bookkeeper` must record an exact model ID such as `gpt-5.6-sol`, `opus5`, or `gemini-3.1-pro`, never `codex`, `claude`, or `agy`. This clarifies DEC-2026-08-13-001 without changing which models are eligible for any role. Human invoked Fast Direct for these Harness edits, waiving the normal planning/review route. | User | `agents/roles.md`, `HERDR.md` |
| DEC-2026-08-13-001 | 2026-08-13 | **Harness model routing is updated.** `opus5` (anthropic) again becomes the default Review-2 model because Claude quota is sufficient; this supersedes only the current routing effect of DEC-2026-08-04-001, whose historical record remains unchanged. All models may give non-formal initial draft review, which cannot replace any required plan review, Review-1, or Review-2 and cannot return an accepting verdict. `agy` (Gemini 3.1 Pro; Google provider) is preferred for that initial review and normally limited to lightweight formal Review-1 unless Human specifies otherwise in the dispatch. Grok 4.6 replaces Grok 4.5 in active Review-1 routing, is suitable for initial operational review and heavyweight Review-1, and remains a Human-approved fallback when Kimi is unavailable. Eligible Bookkeepers are `agy`, Codex/GPT, and Claude; Human explicitly selects the exact canonical model ID at every stage intake, with no automatic default or fallback. Human invoked Fast Direct for these Harness edits, waiving the normal planning/review route. | User | `agents/roles.md`, `HERDR.md` |
| DEC-2026-08-12-002 | 2026-08-12 | **Herdr may be used as a Human-directed cross-terminal copy/paste aid.** A model remains forbidden to act as or impersonate another independent workflow model session. After explicit Human permission, it may use Herdr to notify the terminal for the next task; the permission creates no approval ID, content hash, repository receipt, audit log, or persistent authorization. Human invoked Fast Direct for this reversible Harness trial and waived its normal planning/review route; the previously prepared `2026-08-12-herdr-approved-handoff-v1` planning packet is stale and must not be started. | User / Codex | `AGENTS.md` §3, §6, §7; `agents/roles.md` Shared Rules and Bookkeeper |
| DEC-2026-08-12-001 | 2026-08-12 | **Historical open/close slippage is the actual cross-leg execution spread.** For each stage, separately aggregate spot and perpetual fills to quantity-weighted average prices, select seller/buyer from the real forward/reverse open/close actions, and record `(seller average - buyer average) / min(two averages) × 100` to four decimals. Seller above buyer is positive; missing/non-positive leg data stays `NULL`, real zero is `0.0000`, and preflight `est_price` is excluded. JSTUSDT close-log row 6 was backed up and corrected to open `0.2316`, close `-0.2192`. After the original code passed plan review, review-1 and review-2, O-2 wording was synchronized; Human confirmed the display, invoked Fast Direct for this final text/data delivery, waived its additional review-1/review-2, and authorized commit/merge/push but not deployment or restart. | User | `archive/2026-08-12-hedge-slippage-spread-v1`, delivery `f99795c` |
| DEC-2026-08-10-001 | 2026-08-10 | **Unified-account manual margin repayment v1 is finally accepted and its independent live gate remains enabled.** The only upstream write is signed one-shot `POST /papi/v1/margin/repay-debt`; exact UI `0` omits `amount` for full repayment, positive decimals preserve the debt-asset quantity, and the server fixes USDT as the fallback while Binance spends same-coin assets first. Local SQLite request-id idempotency, four-state recovery, pre-send browser persistence and success refresh are accepted after cross-provider plan review, review-1 and Opus 5 review-2. Live evidence: XLM 5 and INJ full repayment each succeeded once with no errors; INJ returned no actual amount, so full repayment is verified by the refreshed debt balance. Operating limits: use one browser tab, avoid concurrent weight-heavy activity, and reopen if a successful full repayment refreshes to a non-zero debt or automated/multi-device submission is introduced. | User | `archive/2026-08-09-pm-margin-repay-v1`, `docs/api/public-market-contract.md` v0.17 |
| DEC-2026-08-09-001 | 2026-08-09 | **Close tasks use two-stage creation and dispatch.** Creation performs local validation only and atomically inserts a `paused / awaiting_manual_start` card; no exchange read, transfer, attempt, worker, or order is allowed until Human clicks Start. Live dispatch then retains cache-first/live-fallback for required facts, freezes position mode from the origin task (`NULL → BOTH` under the Human premise that mode is unchanged), removes close-only reads with no decision consumer (PAPI/Spot rate-limit and Spot USDT), validates signed UM position with a 300-second cache ceiling plus live fallback, and validates forward ordinary-spot base before every pair using fresh `q_common × remaining_attempts`. Dry-run bypasses the two new live-only gates and remains zero POST. New and legacy-NULL multiplier close tasks are blocked; this supersedes only DEC-2026-08-07-006's earlier statement that close was deliberately left unblocked. | User; plan by Codex/OpenAI; independent plan review by Opus 5/Anthropic | `docs/planning/close-task-preflight-simplification-2026-08-09.v2.review-request-opus5.md`, `docs/planning/close-task-preflight-simplification-2026-08-09.v2.review-opus5-result.md`, `docs/product/PRD.md` §6.3 |
| DEC-2026-08-08-003 | 2026-08-08 | **The CLI-managed-subagent Harness trial is finally accepted and closed without review-2 by explicit Human exception.** Opus 5 review-1 returned `REWORK` for the residual undefined term `delivery team`; the finding was recorded at `b9bcbe1` and repaired with the one-word change `delivery team` → `model` at `f3ef9d2`. Human accepted the repaired delivery, explicitly waived review-2, and declared this work complete. There was no active formal stage (`ACTIVE.json` was already `{"active": null}`), so no stage archive or status transition is fabricated. The rollback snapshot remains `snapshot/harness-before-cli-managed-subagents-20260808` at `a21a340`. | User | `docs/planning/harness-cli-managed-subagents-2026-08-08.review-opus5.md`, commits `b9bcbe1` and `f3ef9d2` |
| DEC-2026-08-08-002 | 2026-08-08 | **The active Harness contains no description or restriction of subagent use.** Subagent orchestration, delegation, nesting, permissions, and lifecycle are left entirely to each model CLI's own architecture. The Harness continues to govern formal workflow roles: a model still cannot start, relay to, assign, or impersonate the next independent workflow session, which the Human starts from a prepared packet. This is a reversible trial; the immediately preceding state is preserved at commit `a21a340` and tag `snapshot/harness-before-cli-managed-subagents-20260808`. | User | `AGENTS.md` §3, `agents/roles.md` Shared Rules |
| DEC-2026-08-08-001 | 2026-08-08 | **Two Harness rules added; completed-work index file deliberately not created.** (1) Every delivery close — including Human-driven, no-stage work — must check whether the living docs under `docs/` (index, product, architecture, dev guide, roadmap, API contract) need updating and sync them: with a stage this is Bookkeeper's duty per §9; without one it belongs to the model recording the close in `PROJECT_STATE.md` (new sentence in `AGENTS.md` §7). (2) Completed work's trace is git history and archive references, not narratives in `PROJECT_STATE.md` — the file's Update Rule now states it records only live risks, open follow-ups, and pointers, and commit messages must state the one-line outcome. A `COMPLETED_STAGES.md` index was considered and rejected: its content is fully derivable from git (archive branches/tags + log), and the v1 `STAGE_INDEX.md` died precisely because it was a derivable copy nobody was contracted to maintain; nor will every development effort be forced through a stage (a bypassed "must" devalues every real "must"). Context: a same-day doc audit found the canonical docs had drifted weeks behind code; both rules target that root cause. Authorized directly by Human, waiving the §8 review topology for these Harness contract edits. | User | `AGENTS.md` §7, `PROJECT_STATE.md` Update Rule |
| DEC-2026-08-07-001 | 2026-08-07 | **The asset-transfer endpoint R1 exposure is accepted as-is.** `POST /api/asset-transfer` moves real money whenever `offline=False` and the hedge API key is set — it is not controlled by `APP_HEDGE_EXECUTOR` and has no independent switch (startup prints an enabled/disabled notice, visibility only). Accepted because production runs live with the Start gate permanently on as the standing premise; every non-offline start assumes the endpoint is reachable. Re-review condition: this exposure must be dealt with before any disabled/offline-mode start, or Human may later decide to add a switch. | User | `PROJECT_STATE.md` Live Risks, `backend/app/server.py` |
| DEC-2026-08-07-002 | 2026-08-07 | **The read-only smoke checklist gate (archive `49-`) is formally voided.** It was registered as a hard prerequisite for the next live activation but was never once executed while live opens, closes, and transfers repeatedly happened around it — a repeatedly bypassed "must" devalues every real "must". Live activation no longer has any prerequisite checklist; the checklist remains in archive `49-` as history only, with no force. The bound note "B-6 does not cover F4's third path" is voided with it. | User | `PROJECT_STATE.md` |
| DEC-2026-08-07-003 | 2026-08-07 | **Spot-symbol resolution switches to the pure table `SPOT_SYMBOL_MAP`** (71 entries = 65 bStock + 6 multiplier contracts, generated from live exchangeInfo): `exact → table → (None, None)`, with all string-guessing rules deleted and fail-closed semantics (unlisted means no spot leg — rather no leg than a wrong leg). The guessing rules had two real defects (`BUSDT` base `B` mismatched to BounceBit's `BBUSDT`; `base[4:]` stripped `1000000MOG` to `000MOG`). Errata: `MUUSDT` and `MUUUSDT` are both real, coexisting contracts (→ `MUBUSDT` / `MUUBUSDT`); the earlier "MUUUSDT is a typo" judgment was wrong. Maintenance: `scripts/check-spot-symbol-map.py --verify/--emit`. | User | `backend/domain/normalize.py`, `scripts/check-spot-symbol-map.py`, `docs/planning/symbol-identity-unification-2026-08-07.opus5.md` |
| DEC-2026-08-07-004 | 2026-08-07 | **Final form of the transfer panel's 「可转/可用」 labels: the frontend makes zero requests and reuses the backend account-cache snapshot.** USDT uses account-level `total_available_balance_usdt` and is labelled 「可转」 (valid only because USDT is the quote unit — verified against the exchange UI and `maxWithdraw` within 0.05%); all other assets use `cross_margin_free` and are labelled 「可用」, which is deliberately not the withdrawable amount. The wording follows the data source (`row.kind`). The real-time `maxWithdraw` path was dropped because it has no batch form (measured 9.68s serial for 18 assets); the batch endpoint `GET /api/private-account/max-withdraw` stays in the backend with no frontend consumer. | User | `PROJECT_STATE.md` |
| DEC-2026-08-07-005 | 2026-08-07 | **WeChat notification and the open-task linkage are cancelled** and are no longer follow-up items (both were listed as deferred items of the dual-ledger-flow-log stage). | User | `PROJECT_STATE.md` |
| DEC-2026-08-07-006 | 2026-08-07 | **1000x multiplier contracts: fail-closed blocking now, leg-quantity conversion awaits explicit Human authorization.** `create_task` rejects `open` tasks whose `symbol_match_type` is `multiplier_strip_alias` (`multiplier_contract_unsupported`) because both legs send one shared `q_common` while 1 contract = 1000 spot units (a 999x net short exposure); close is deliberately not blocked (no such positions exist, and auto-close has the same wrong sizing anyway). Restoring hedging for the 6 multiplier coins (BONK/FLOKI/LUNC/PEPE/SHIB/XEC) requires changing the real order-quantity path — the eight-site list in `PROJECT_STATE.md` Open Follow-ups — and must run as its own authorized round, never smuggled into other work. The multiplier must be carried per-entry by `SPOT_SYMBOL_MAP`, not hard-coded (`1000000MOG` exists). | User | `PROJECT_STATE.md` Live Risks / Open Follow-ups |
| DEC-2026-08-07-007 | 2026-08-07 | **Domain fact: bStock coins have no borrow market, so negative-funding (reverse) opens do not exist for them.** A reverse open is borrow-coin-and-sell-spot plus a long perp; without a borrow market the strategy cannot be entered. Consequence: every `reverse + bStock` code path is unreachable, and fail-closed behavior there is correct, not a defect. | User | `PROJECT_STATE.md` Current Status |
| DEC-2026-08-07-008 | 2026-08-07 | **`PROJECT_STATE.md` budget raised from 32 KB to 64 KB.** Done inside the housekeeping commit `dc613c1` (outdated-record cleanup) without a separate decision row at the time; recorded here after the fact. The active authority remains the one sentence in `PROJECT_STATE.md`; this row is decision history only. | User | `PROJECT_STATE.md`, commit `dc613c1` |
| DEC-2026-08-06-001 | 2026-08-06 | **Live data wipe authorized as a from-scratch test starting point.** All rows cleared in the three databases (hedge-open-tasks 447 rows, borrow-tasks 1299 rows, ledger-flow 2285 rows); table structure and settings (start_gate/close_gate config) kept; backups at `data/*.sqlite3.bak-clean-20260806-120813`. Exchange positions were all manually closed by Human beforehand. | User | `PROJECT_STATE.md`, `data/*.sqlite3.bak-clean-20260806-120813` |
| DEC-2026-08-04-001 | 2026-08-04 | **Default review-2 model changed from Opus 5 to `sonnet5` (anthropic)** for Claude-quota reasons; review-1 routing is unchanged (no global default — `kimi` stays the preferred cross-provider review-1 model for `claude_glm` implementation per `agents/roles.md`). Kimi quota was unavailable until 2026-08-07 — before that date review-1 routed to `deepseek` or `codex` instead. | User | `PROJECT_STATE.md` |
| DEC-2026-08-03-001 | 2026-08-03 | **`PROJECT_STATE.md` budget raised from 8 KB to 32 KB.** The current file reached 8,026 bytes, leaving almost no room for still-open cross-stage risks and follow-ups. Human authorized this direct fast modification and commit on `main`; independent review is waived for this single numeric Harness-limit change. The next stage will assess and compact the file. The active authority remains the one sentence in `PROJECT_STATE.md`; this row is decision history only. | User / Codex | `PROJECT_STATE.md` |
| DEC-2026-07-31-002 | 2026-07-31 | **`stage_id` naming is defined for the first time**: `YYYY-MM-DD-<kebab-case description>-v<N>`, the date being the local date the stage is opened, and the stage directory name must equal `stage_id`. Previously the format existed nowhere — not in `AGENTS.md`, `agents/roles.md`, nor any file v2 deleted (v1's `validate-stage.py` never referenced `stage_id` at all); it propagated by imitation, and 7 of 43 existing directories had already drifted off it. Adding the day improves browsing and sort order. Stated limit, deliberately written into the rule: a stage often spans days, so the date marks only the start, not what changed that day — `git log --since/--until` is the authority for a per-day view. **Existing stages are never renamed**: their names are referenced by archive tags, `PROJECT_STATE.md` git pointers, `DECISIONS.md` source columns and `STAGE_INDEX.md`, and renaming would break traceability. Known cosmetic side effect: day-stamped ids sort before the older month-stamped ones until those are cleaned up (G11). Authorised by Human as a direct fast-fix edit (dual-hat: Opus 5 as both Bookkeeper and implementer), explicitly waiving the §8 `HIGH_RISK` review topology. Authority for the rule is the sentence in `agents/roles.md`; this row is decision history. | User / Opus5 | `agents/roles.md`, archive `archive/2026-07-harness-v2-trial-hardening-v1` file `22-` §23 |
| DEC-2026-07-31-001 | 2026-07-31 | **`PROJECT_STATE.md` budget raised from 2 KB to 8 KB.** The 2 KB cap overflowed twice within one hour on 2026-07-31 — once closing a stage, once when two concurrent stages each added a line — the second time forcing eviction of another stage's still-open follow-up. Raising the number is a deliberate usability-first choice; optimisation is deferred. Trade-offs recorded and accepted: (a) the eviction rule at the foot of `PROJECT_STATE.md` triggers on "over budget", so at 8 KB it fires roughly four times less often and the file may reach ~4x today's size before anything is pruned; (b) `AGENTS.md:61` targets ~8K tokens for startup, and an 8 KB `PROJECT_STATE.md` alone could consume a large share of that, which touches preserve rule W1. Authorised by Human as a direct fast-fix edit (dual-hat: Opus 5 as both Bookkeeper and implementer), explicitly waiving the §8 `HIGH_RISK` review-1 + review-2 topology a Harness contract change normally requires. The active authority for the number remains the one sentence inside `PROJECT_STATE.md`; this row is decision history, not authority. | User / Opus5 | `PROJECT_STATE.md`, archive `archive/2026-07-harness-v2-trial-hardening-v1` file `22-` §22 |
| DEC-2026-07-22-006 | 2026-07-22 | Market-table filters: **日费率** hide uses **absolute value** `\|daily\| ≤ 0.03%`. **日净收益** hide uses **signed** `net ≤ 0.03%` (all negatives and small positives hidden; null not hidden). Labels: keep `\|日费率\|`; net filter text is `隐藏 日净收益 ≤ 0.03%` without bars. Both filters are frontend-only and stack. | User / Grok | `frontend/index.html`, `docs/planning/CHANGELOG-2026-07-22-market-table-filters.md` |
| DEC-2026-07-22-005 | 2026-07-22 | **可开优先展示** (default on): after existing filters, re-rank only — basket A = positive funding rate, or negative rate with verified borrowability and `max_borrowable > 0`; sort A by `net_daily_yield` DESC. No extra route/spot-leg gate; PERP_ONLY visibility remains the existing show-PERP checkbox. Unprobed / max=0 stay out of A. Not a hard filter. | User / Grok | `frontend/index.html`, `docs/planning/CHANGELOG-2026-07-22-market-table-filters.md` |
| DEC-2026-07-22-004 | 2026-07-22 | Snapshot row field **`funding_sum_24h`**: inclusive settled-rate sum over `[data_time−24h, data_time]` (not annualized); table column **近 24h** left of 年化 24h, display 3 decimal percent. Edge points counted once. Additive schema; missing field normalizes to null on frontend. | User / Grok | `backend/domain/snapshot.py`, `schemas/api/public-market/snapshot.schema.json`, `docs/planning/CHANGELOG-2026-07-22-market-table-filters.md` |
| DEC-2026-07-22-003 | 2026-07-22 | Live borrow **Scheme C**: transport-layer failures (`timeout`, `connection_error`, missing HTTP status) classify as `known_rejection` so tasks stay in scheduler rotation. Operator accepts possible over-borrow when a POST may have reached Binance without a local response, for high-frequency empty-pool hunting. 5xx and malformed 2xx remain `unknown` + recon. | User / Grok | `backend/services/live_borrow_executor.py`, `docs/planning/CHANGELOG-2026-07-22-live-borrow-ops.md` |
| DEC-2026-07-22-002 | 2026-07-22 | Live borrow **Scheme A** (4xx): every definite HTTP 4xx after rate-limit handling is `known_rejection` (task not blocked). Includes pool codes 51006/51014/51061 with either wire sign, and auth rejects such as HTTP 401 / `-2015`. Rate limits 429/400+`-1003`/418 unchanged. | User / Grok | `backend/services/live_borrow_executor.py`, `docs/planning/CHANGELOG-2026-07-22-live-borrow-ops.md` |
| DEC-2026-07-22-001 | 2026-07-22 | Borrow attempt **log coalesce**: if this task’s last result is the same coalesceable failure (`known_rejection` / `rate_limited` / `execution_disabled` with identical reason + business_code), do not keep a second log row — only refresh the previous failure’s `finished_at`. Success always keeps its own row; unknown never coalesces. | User / Grok | `backend/borrow_tasks/store.py`, `backend/borrow_tasks/domain.py`, `docs/planning/CHANGELOG-2026-07-22-live-borrow-ops.md` |
| DEC-2026-07-03-001 | 2026-07-03 | Archive the Grok public-market implementation and restart Phase 1 from the approved PRD with a contract-first split: Claude-GLM owns backend Binance public API contract discovery and backend implementation; Kimi owns frontend UI and integration against the frozen backend contract; Grok is excluded from core backend and contract work for this phase. | User / Codex | `reports/archives/2026-07-03-grok-public-market-discovery/`, `reports/agent-runs/2026-07-public-market-contract-v2/11-adr.md` |
| DEC-2026-07-05-001 | 2026-07-05 | Adopt stage-branch mode: from the next stage onward, the bookkeeper creates `stage/<stage-id>` at H_intake, all stage commits and fingerprints anchor to the branch, and the branch merges back to main only after user acceptance; Harness/template syncs stay on main. Execution deferred until the in-flight stage `2026-07-phase2-borrow-sort-v1` completes (its fingerprints are bound to main commits). The rule change lands template-first (ai_project_harness) with GPT review, then syncs down. | User / Fable5 | `docs/planning/stage-branch-mode.md` |
| DEC-2026-07-08-001 | 2026-07-08 | Update Harness model routing: Codex/GPT is no longer an implementation or fix author; Claude-GLM owns backend/API/contract/schema/data-semantics work; Kimi owns frontend/UI/client-integration work; dominant mixed tasks may be dispatched wholly to the dominant owner; Claude uses Fable5 first and Opus4.8 after Fable5 quota exhaustion. | User / Codex | `AGENTS.md`, `agents/registry.yaml`, `docs/model-adapters.md`, `workflows/templates/stage-delivery.yaml` |
| DEC-2026-07-08-002 | 2026-07-08 | Codex/GPT and Claude provider sessions may prepare model-facing dispatch packets, prompts, routing metadata, and handoff text, but must not execute model-dispatch commands or invoke implementation/review/fix model terminals. Human operators execute prepared dispatch packets in the selected target model terminal and record raw output or receipt evidence. | User / Codex | `AGENTS.md`, `agents/registry.yaml`, `docs/model-adapters.md`, `workflows/templates/stage-delivery.yaml` |
| DEC-2026-07-10-001 | 2026-07-10 | Supersede and narrow DEC-2026-07-08-002: the dispatch ban applies to prepare-only sessions invoking other model terminals or adapter commands. Default human delivery is interactive paste or a read-file-and-execute-PROMPT-BODY instruction. A selected target executor session that receives the dispatch path or PROMPT BODY must execute in-session and must not emit a same-provider/model relaunch wrapper. CLI commands are optional automation appendix material for runners, evidence capture, availability checks, or declared executor:self automation, not the default human UI. | User / Codex / Grok review | `AGENTS.md`, `agents/registry.yaml`, `docs/model-adapters.md`, `docs/harness-design.md`, `docs/parallel-development-mode.md`, `workflows/templates/stage-delivery.yaml` |
| DEC-2026-07-10-002 | 2026-07-10 | Roll the Harness contract back to the DRAFT-2 decision baseline: canonical Harness files restored to their `main @ 41c6ba5` state as frozen in `reports/agent-runs/2026-07-harness-flow-optimization-v1/05-harness-environment-snapshot.md`. This rescinds the file-level effects of the §14 ITBM enablement (record-checkpoint / prepare-review-2 / _itbm.py / ITBM schema, docs, templates, validator support), the harness-friction-fixes and harness-hardening-followups deliveries, and DEC-2026-07-10-001 (paste-first dispatch semantics; DEC-2026-07-08-002 human-executed dispatch is in force again). D1–D4 remain locked decisions in the snapshot and may be re-implemented later. Kept on top of the baseline: truthful Project State wording, the STAGE_INDEX.md convention, and the reporting-language preference section. Pre-rollback state preserved at branch `archive/harness-pre-rollback-2026-07-10` (main `0daaa81`) for retrospection; stage `2026-07-harness-promotion-gate-upsync-v1` abandoned unmerged. Reason: two days of Harness churn drifted the project away from product work. | User / Fable5 | `reports/agent-runs/2026-07-harness-flow-optimization-v1/05-harness-environment-snapshot.md` |
| DEC-2026-07-14-001 | 2026-07-14 | Change the `auto-review-pipeline/v1` default runner host from a persistent Kimi model session to the human operator's normal shell. The human directly starts and watches `python3 scripts/auto-review-runner.py <stage-id>`; the deterministic runner remains the sole automatic dispatcher and mechanical writer. Kimi remains eligible only as a fresh runner-created implementation/review fallback. This prevents an outer model-host timeout from terminating the runner independently of committed adapter timeouts. | User / Codex | `AGENTS.md`, `agents/registry.yaml`, `docs/auto-review-pipeline.md` (deleted; retired by DEC-2026-07-14-002), `docs/model-adapters.md`, `workflows/templates/stage-delivery.yaml`, `scripts/auto-review-runner.py` (deleted; retired by DEC-2026-07-14-002), `scripts/validate-stage.py` |
| DEC-2026-07-14-002 | 2026-07-14 | Retire `auto-review-pipeline/v1` after its cache-refresh pilot produced no delivery-code changes and exposed write-authorization, empty-diff, blocker-routing, seal-ordering, and stale-state defects. Restore the canonical Harness files from the existing `540513d` implementation of the DRAFT-2 decision baseline instead of resetting product history to `41c6ba5`. Preserve all product code, product documents, historical stage evidence, the environment snapshot and `status.json` pointer, and retain only the lightweight `ACTIVE.json` plus Recovery Header startup fast path as a deliberate post-baseline usability addition. The failed `2026-07-cache-refresh-scheduler-v1` automatic attempt is abandoned without merge; its approved task/design/ADR/development-breakdown documents and raw incident evidence remain on the retained stage and archive branches for migration into a new manual-delivery stage. | User / Codex | `reports/agent-runs/2026-07-harness-flow-optimization-v1/05-harness-environment-snapshot.md`, `reports/agent-runs/2026-07-harness-flow-optimization-v1/status.json`, `archive/harness-pre-rollback-2026-07-14`, `archive/cache-refresh-scheduler-pre-harness-rollback-2026-07-14` |
| DEC-2026-07-30-001 | 2026-07-30 | **Unknown is not zero** (order records): a figure the exchange did not return is stored and projected as `NULL`/`None`, never coerced to `0`; a real exchange `"0"` still means zero. Quantity is explicitly excluded — `cumulative_base_qty` is `TEXT NOT NULL DEFAULT '0'`, so honesty there needs a live-table rebuild, and a leg never sent genuinely has zero fill. One implementation of the leg-exposure rule only (`domain.build_leg_exposure`); the store delegates. A static tripwire (`find_money_zero_defaults`) guards money identifiers against zero-defaulting constructs — **a speed bump against accidental reintroduction, not a proof**: five evasions are known (`or Decimal(0)`, `or 0`, ternary, laundering via an intermediate variable, `fee_amount` outside the money names) and it cannot detect a migration that over-nulls a real `'0'`. | User / Opus5 plan, Codex plan review, GLM impl, Grok + Codex review | `backend/hedge_open_tasks/store.py`, `backend/tests/test_hedge_purity.py`, archive `archive/2026-07-unknown-not-zero-v1` |
| DEC-2026-07-30-002 | 2026-07-30 | **A discarded failure is a defect** (hedge state writes): a failed `self._store.*` write that changes what the system believes about an order, money, or task eligibility must record an operator-visible event and must never fabricate a conclusion or resend. Two event kinds, both persisted and distinct: `settlement_failed` (settlement paths) and `state_write_failed` (other state writes, carrying `operation`). Losing only a log line is a different class and a narrow guard around that audit write is correct. Judged **not** statically defensible — no rule catches `verdict is None`, thread-exception conversion, wrappers, aliases, or set-flag-then-return — so the defence is a closed audited list plus per-site fault-injection tests plus cross review. | User / Codex audit, GLM impl, Grok + Codex review | `backend/hedge_open_tasks/service.py`, `backend/tests/test_hedge_task_local.py`, archive `archive/2026-07-unknown-not-zero-v1` |
| DEC-2026-07-30-003 | 2026-07-30 | **Row-mutating migrations do not fire implicitly.** `HedgeOpenStore.__init__` runs additive DDL automatically but the surviving data repair (M2, the `leg_exposure.ts` 1970 rewrite) runs only under an explicit default-off argument that no production caller passes. Buys "no semantic row rewrite on default construction", **not** "never writes the file" — additive DDL still writes. Prompted by the 2026-07-28 incident where constructing a store against the production database rewrote two rows with no caller intending it. | User / Opus5, GLM impl | `backend/hedge_open_tasks/store.py` |
| DEC-2026-08-02-001 | 2026-08-02 | **Order re-query cadence is 500ms with a 10-try retry budget, not the 100ms originally requested.** Human switched to 500ms to match the legacy JS strategy, whose `getSpotOrderInfo` retries 10x500ms and whose `checkOrder` waits 500ms before its first poll. A `404`/`-2013` from the order-detail query is **no longer believed on the first try**: each leg gets `LEG_QUERY_MAX_RETRIES` attempts before it settles as absent, and a leg still inconclusive at the cap escalates to a manual-verification pause (`order_state_unknown`) rather than being equated with absence - mirroring the protection `_confirm_um_figures` already documented for the POST-confirm path (a POST-just-accepted order 404-ing is eventual-consistency noise). The retry count is **in-process**, matching the legacy loop; a restart re-counts. Request-budget consequence: the worker queries every non-terminal leg each round, so two legs in flight is ~4 req/s per task - **run at most ~5 tasks draining concurrently** against Binance's ~20/s weight budget. | User / Opus5 bookkeeping, deepseek plan-check + impl, GLM impl, codex + gpt review-1 (3 rounds), Fable5 review-2 | `backend/hedge_open_tasks/{domain,service,store}.py`, archive `archive/2026-07-31-hedge-task-lifecycle-v1` |
| DEC-2026-08-02-002 | 2026-08-02 | **The re-query cadence has exactly one source of truth: the code constant.** `get_interval_us()` returns `D.DEFAULT_INTERVAL_US` and never reads `hedge_open_settings`; the interval columns remain as **dead data** (no endpoint, UI or API could ever write them, which is what made them useless as a source). Reason: keeping a second copy in the database forced a row-rewriting migration to change the cadence, that migration ran unconditionally inside `_migrate` in violation of DEC-2026-07-30-003, and it silently changed the running production database on 2026-08-01 (BK-T3-002). Removing the second source removes the migration, so **constructing a store against any database never writes to it** - asserted by a byte-level regression. The columns are deliberately NOT dropped: a schema migration against the live DB is the very risk this change exists to remove. Cost accepted: the cadence is no longer runtime-configurable - it never actually was. | User (authorised skipping review) / Opus5 implementation | `backend/hedge_open_tasks/store.py`, `backend/hedge_open_tasks/service.py`, `backend/tests/test_hedge_store.py` |
| DEC-2026-08-02-003 | 2026-08-02 | **The hedge task lifecycle rework (deadlock fix + five-reason auto-delete + `rate_limited` backoff + the `COALESCE` guard) is designed, plan-reviewed and deliberately not built.** Its design, the approved decisions behind it (five-delete-one-backoff per ADR-002/D16), the five reservations that surfaced on review - including that the deadlock fix makes the dishonest `done` label *more* frequent and less visible, that `exposure_alert` is a status nothing writes, and that the task card renders 1 of 7 pause reasons in Chinese - and the one confirmed input (`order_state_unknown` stays a manual pause) are recorded in `docs/planning/deferred-hedge-task-lifecycle.md`, which is self-contained so restarting never needs the archived stage. F4's fix is fully specified and should be scheduled on its own rather than waiting for this. | User / Opus5 | `docs/planning/deferred-hedge-task-lifecycle.md`, archive `archive/2026-07-31-hedge-task-lifecycle-v1` |
