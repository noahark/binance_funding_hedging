只读评审任务（第四轮）。不要修改仓库任何文件，不要实现代码。

对象：docs/planning/leg-unit-size-conversion-2026-08-15.opus5.md（**r4**）
仓库：/Users/ark/Desktop/ai code/funding_hedging，分支 main，基线 f00df2b
状态：设计稿，生产代码一行未改。六个乘数币目前被三道 fail-closed 拦截挡着，零风险敞口。

派发对象：三方独立评审，互不参考。经 Herdr 派往各自的 `-review` 窗口。

| 窗口标签 | 实际评审模型（DEC-2026-08-13-002：按模型记名，不按窗口标签） |
|---|---|
| `grok-review` | Grok |
| `claude-glm-review` | Claude Code using GLM |
| `agy-review` | **`gemini-3.7-flash`**（Human 于派发时说明；`HERDR.md:23` 记的「预期 `gemini-3.1-pro`」不是本轮实际） |

---

## 背景

六个乘数合约（1000BONK / 1000FLOKI / 1000LUNC / 1000PEPE / 1000SHIB / 1000XEC）的
合约腿 base asset 是一个合成单位（`1000BONK` = 1000 个 `BONK`），而现货腿是 `BONK`。
系统给两条腿发同一个数量，故 2026-08-07 起 fail-closed 挡住。本方案要放开它们。

评审历史：
- r1 → claude-glm + grok 双评审，均 REWORK
- r2 → grok 评审 REWORK（claude-glm 撞 provider 内容过滤 400 未完成）
- r3 → 未派评审。Human 提问 + 作者自查后直接进入 r4
- r1/r2 评审结论在同目录：`*.review-claude-glm-result.md`、`*.review-grok-result.md`、
  `*.review-grok-result-r2.md`

**本轮尤其需要外部视角**：r3 之后没有任何外部模型看过，r4 的变更全部来自 Human 提问
与作者自查，存在「作者自己给自己背书」的结构性风险。

---

## r4 相对 r3 的七项变更（评审重点）

| # | 变更 | 性质 |
|---|---|---|
| A | **全稿删除「张」这个词。** Human 指正：币安 U 本位合约没有「张」，`quantity` 填的就是 base asset 个数，只是乘数币的 base asset 是合成单位 `1000BONK`。新增 §2.5 说明 `face_value` 是将来接别家交易所时的**独立第二乘数**（`y = 倍数 × face_value`），币安恒为 1，**本轮不建任何机制**，仅约束新列不得命名 `contract_size` | 术语纠正 + 前瞻边界 |
| B | **§3.4 / §3.5 各补一个 reverse 分支**（`domain.py:1591`、`snapshot.py:699`）。r3 只列了 forward 分支，而 r3 自称「实现者只抄表也不会漏」——该承诺不成立 | 漏项补全 |
| C | **§3.5 持仓表那处改法整体替换**：由「前端 JS 加乘数」改为「**后端在 `aggregate_positions`（`store.py:2834-2836`）源头换算 `perp_avg`**」，前端一行不改 | 方案变更 |
| D | **新增 §2.6 口径规则**：对账数字不换算（只加单位标注）/ 比较数字才换算 | 新口径 |
| E | **§9 交付顺序重排为 A–H**，把「拆假执行器」提为独立第一阶段（先让测试能够失败，再改实现） | 排期变更 |
| F | **§6 判定前置轮「改动二」不并入本轮**，推翻前置轮文档的「建议并入」 | 范围决策 |
| G | **新增 §0.5 成本/收益实测数据** | 决策依据 |

---

## 七个必答问题（即 r4 §11，此处复述以便直接作答）

**【问题 1】§3.4 / §3.5 的 reverse 分支是否补全？还有没有第五处？**

作者自查结论（**请独立复核而非采信**）：「两腿价差」确为 3 份独立实现——盘口
（`snapshot.py:613` 的 `compute_opening_spread_pct`）、后端成交记录
（`store.py:2563` `cycle_slippage_pct`）、前端成交记录（`index.html:5249`
`computeHedgeOpenBasisRate`）。r3 的「三处」判断成立，但每处的 **reverse 分支未被列名**。

同时核查并排除了三项：
- `service.py:1822` 的滑点门路由进 `evaluate_smooth_gate`，§3.4 已覆盖
- `frontend/self-check.js` 只有 fixture，无独立公式
- `aggregate_positions` 的 `open_basis_rate` 硬编码 `"0"`，前端不读该字段

请独立搜索是否仍有第四份实现，或第五个未列名的分支（含前端、后端、自检脚本、
任务卡日志、导出/报表）。

**【问题 2】§3.5 第 3 项的 r4 改法（后端源头换算 `perp_avg`）是否安全？**

依据是一次 grep 结论：`perp_avg` 的消费者只有 `index.html:6731`（合约均价列）
与 `index.html:6624`（价差率入参），**后端零消费**。

请独立验证该结论，并判断是否存在通过 API 契约、导出、报表、日志间接消费该字段
而作者未搜到的路径。**若该前提不成立，本改法会把一个纯展示问题变成数据问题。**

**【问题 3】§2.6 的规则（对账数字不换算 / 比较数字换算）是否自洽？**

是否存在**既需与交易所对账、又需跨腿比较**的量？若存在，该规则自相矛盾。

特别请核对持仓表：`perp_avg`（按本规则换算）与 `um_position_amt` / `position_qty`
（按本规则不换算）同处一行，读者是否会被误导。

**【问题 4】§2.5 的 `face_value` 判断是否成立？**

本稿主张：币安 `face_value ≡ 1`，故本轮 `y = 倍数` 直接成立，将来叠乘即可，
**因此不建任何机制、不留占位**。

请核对该主张对币安 U 本位永续是否普遍为真（含 `1000000` 前缀合约、bStock
`TRADIFI_PERPETUAL`），以及「不留占位」是否会在将来造成不可逆的返工。

**【问题 5】§9 的排期（A 阶段先行）是否成立？**

依据两条实测事实：
- `backend/tests/fakes.py` 的成交模拟运行时不可达（`service.py:3665` 明载，
  2026-08-06 Human 决定移出生产；生产代码对 `fakes` 零引用）
- `OutcomeSpec`（`fakes.py:37`）已有 `spot_qty` / `perp_qty` 两腿各自的数量覆盖字段
  （`fakes.py:48-49`）

请核对；并判断 A1 的默认值设计能否真正做到「现有 1939 条测试零影响」。

**【问题 6】§0.5 的成本/收益是否改变结论？**

实测（2026-08-15 币安公开 API）：可对冲宇宙 358 → 364（+1.7%）；11 个月窗口按
`|费率| >= 0.05%` 阈值，六币合计 189 次机会、边际贡献 **1.72%**，其中 **74% 集中于单一
标的 `1000XEC`**，`1000SHIB` 在 500 个周期内**从未达阈**。

作者结论：**值得做，但理由是「修 bug + 拆脚手架」而非这 1.72%**——现存止血脚手架
（生产约 12 处 + 测试约 31 行）在本轮结束时全部删除，净新增维护面约 3 处；且因
「无 `if`、倍数 = 1 恒等」的形状，另外 352 个标的的代码路径一行不变。

**请独立判断：这是否足以支撑一轮触及下单数量路径的改动？若认为不值得，请直说。**

**【问题 7】本稿是否还有引用错误？**

r1 有一处（`_is_close_flat` 不存在），r2 有一处文档自相矛盾（`2133`），
r3 有两处漏列分支。**请实跑 §8.1 校验脚本并报告结果。**
r4 已把本轮新增的 13 条引用纳入该脚本，作者于基线 `f00df2b` 实跑为 **46/46 OK**。

注意：该脚本只证「行号命中」，**不证「改法正确」**。

---

## 请特别留意的自证风险

本方案要处理的是**量纲错误**，其特征是：**不报错、只算错，且实现与测试极易共用同一套
错误假设而全绿**。r4 §8.2 有四条反自证约束（A/B/C/D）。请判断是否仍有残余路径。

同时请注意 r4 自身的一个结构性弱点：**r3 之后无任何外部模型评审过**，r4 的全部变更
来自作者自查。作者自查在 r3 上找出了两处漏项（`1591` / `699`），这既说明自查有效，
也说明**三轮评审 + 一轮自查后仍在持续发现同一形状的遗漏**。请假定还有。

---

## 输出要求

按仓库 `AGENTS.md` 的评审口径给出 **ACCEPT** 或 **REWORK**，逐条列出问题并附文件路径
与行号。七个问题必须逐一正面回答。若判 REWORK，请明确哪些是阻塞项、哪些是观察项。

约束：只读；不要改任何文件；不要实现；结论写在你的回复里。
