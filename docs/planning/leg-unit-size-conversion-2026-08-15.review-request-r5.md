只读评审任务（第五轮）。不要修改仓库任何文件，不要实现代码，不要落盘。

对象：docs/planning/leg-unit-size-conversion-2026-08-15.opus5.md（**r5**）
配套必读：docs/planning/leg-unit-size-conversion-2026-08-15.column-inventory.md（逐列清单，25 格）
仓库：/Users/ark/Desktop/ai code/funding_hedging，基线 main @ 0e2b4ff
状态：设计稿，生产代码一行未改。六个乘数币仍被三道 fail-closed 拦截挡着，零风险敞口。

派发：三方独立评审，互不参考。经 Herdr 派往各自 `-review` 窗口。

| 窗口 | 实际评审模型（DEC-2026-08-13-002：按模型记名） |
|---|---|
| `grok-review` | Grok |
| `claude-glm-review` | Claude Code using GLM |
| `agy-review` | `gemini-3.7-flash` |

---

## 上一轮（r4）三方结论与本轮处置

| 评审 | r4 结论 | 本轮说明 |
|---|---|---|
| **grok** | **REWORK，2 阻塞** | 两项均已处置，见下 |
| `claude-glm` | ACCEPT，0 阻塞 6 观察 | 六项全部采纳；其 O2 与 grok F1 是同一问题，**r5 判定它应为阻塞项而非观察项** |
| `agy` (`gemini-3.7-flash`) | ACCEPT，0 发现 | **其问题 3 答案（称持仓表「无误导性」）经核实为错**，持仓表确有冲突。该轮不作为独立证据采信 |

**grok 两个阻塞项的处置：**

- **F1**（源头换算 `perp_avg` 会改脏公开字段并与同表交易所价格对不上）
  → **采纳 A 方案**：撤回 r4 改法，`perp_avg` 保持原单位 + 单位标注，
  只在 `index.html:5257/5258` 两支换算，倍数走 §3.3 已有的下发管道。见 r5 §3.5。
- **F2**（§2.6 须改成逐列清单，「没有这张表不要开工」）
  → **采纳**：§2.6 散文规则整节作废，改为清单指针 + 三条硬约束；
  清单本体为配套文件，**25 格 / 6 界面**。

**grok 独家发现的历史页整张表** → 新增 r5 §3.7，含「三格禁止换算」警告。

---

## r5 相对 r4 的六项变更

| # | 变更 | 性质 |
|---|---|---|
| A | **§3.5 撤回 r4 的源头换算**，改用 grok F1 的 A 方案 | 阻塞项修复 |
| B | **§2.6 散文规则 → 逐格清单**（配套文件 25 格） | 阻塞项修复 |
| C | **新增 §3.7 历史页与任务卡**，含三格「禁止换算」 | 新增改造面 |
| D | **§2.3 拆为 2.3.1 计算路径 / 2.3.2 展示路径**，展示层默认改为「原样」 | **设计更正** |
| E | **§0.5.1 立项理由重写**，由「当期收益」改为「能力可用性」 | 立项依据 |
| F | §8.1 补 8 条至 54 条；§8.2 补用例 15–17；§9-D 拆为 D1–D5 含倍数下发管道 | 验收与排期 |

---

## 本轮尤其要防的一件事（请先读）

r4 轮暴露了一个具体规律：**每一轮都还在发现同一形状的遗漏，且发现者每轮不同。**

```
r1  漏「两腿价差」全部三处
r2  漏第三处（前端）                  ← grok 找到
r3  漏两个 reverse 分支               ← 作者自查找到
r4  漏历史页与任务卡整两个界面         ← 仅 grok 找到，作者与另两家全漏
```

r5 的应对是把散文规则换成逐格清单。**请假定清单仍不全，
并把「你数出第 26 格」当作本轮最有价值的产出。**

---

## 七个必答问题（即 r5 §11）

**【问题 1】§2.6 配套清单（25 格 / 6 界面）是否数全？**

作者已核实为空的面见清单 §8（持仓表不渲染 `spot_qty`/`perp_qty`、无导出/CSV、
预检快照前端不渲染、`store.py:2061` `list_fills_for_task` 无调用者）。

**请独立枚举，不要按语义搜**——语义搜正是漏掉历史页的原因（历史页不算价差，
搜 `spread`/`basis`/`slippage` 零命中）。重点补搜方向：任务卡其余区块、
告警/通知文案、日志落盘、`server.py` 其它端点、`self-check.js` 的断言期望值。

**【问题 2】§3.7.1 的「三格禁止换算」是否正确且完备？**

本稿主张历史页两个成交额（`index.html:5386` / `:5391`）与逐腿明细的累计成交额
（`cumulative_quote_amt`）**今天就是对的**：

```
合约成交额 = open_qty (个 1000BONK) × open_avg_price (每个 1000BONK) = 正确 USDT
             ↑ 两个量纲错误方向相反，乘积正确
```

请验算；并搜索是否还有第四格具有同样的「乘积自洽」性质
（提示：任何 `逐腿数量 × 该腿价格`，或交易所直接下发的 USDT 金额）。

**这是全轮唯一「改了反而错」的失败模式**，其余所有格子都是「不改就错」。

**【问题 3】§3.5 撤回源头换算、改用 A 方案，是否解决了 grok F1？**

A 方案：`perp_avg` 保持原单位 + 单位标注，只在 `index.html:5257/5258` 两支换算。
请核对：(a) 持仓表五格（`um_position_amt` / `um_entry_price` / `um_mark_price` /
`um_liquidation_price` / `perp_avg`）是否恢复内部一致；
(b) 公开契约 `public-market-contract.md:1981` 是否确实无需改动；
(c) 是否引入了 r4 没有的新问题。

**【问题 4】§2.3.2 的设计更正是否成立？**

本稿主张「计算路径必须换算、展示路径默认原样」，依据是 25 格中 22 格不该换算。

**请判断该二分是否会在某个边界失效**——特别是既进计算又上界面的中间量
（如平滑门盘口展示 `index.html:6190-6201`，本稿判定为「原样展示 + 比较副本」）。

**【问题 5】§0.5.1 的立项理由是否成立？**

grok r4 判「1.72% 不够支撑动下单数量路径，工程理由要等 F1/F2 写清才够」。

r5 主张 F1/F2 已写清，且据 Human 决定「**XEC 现在不碰不代表以后不碰**」，
立项理由改为三条与当期收益无关的：
(1) 这是修 bug 不是加功能——现状是半套已建、靠拦截止血，不是终态；
(2) 拆 12 处生产脚手架 + 31 行测试是净收益；
(3) 机会是尖峰型，**尖峰到来时临时开工来不及**，而成本与哪个币当期活跃无关。

**请独立判断这三条是否足够。若仍认为不足，请直说。**

**【问题 6】§0.5 的工作量重估（500–800 行）是否可信？**

r4 的 400–600 行基于「展示 3 处」已作废。请核对新估算是否遗漏了
§9-D2 的倍数下发管道、D4 的约 19 格单位标注、以及测试改造的真实成本。

**【问题 7】本稿是否还有引用错误或自相矛盾？**

历轮均有：r1 `_is_close_flat` 不存在；r2 `2133` 自相矛盾；r3 漏列两个分支；
r4 文首 33/33 而正文 46/46（grok 观察）。**作者在 r5 修订过程中又险些重犯第四次**
（扩脚本至 54 条后四处仍写 46/46，已修）。

**请实跑 §8.1 校验脚本并报告结果**——r5 已补入 8 条至 54 条，
作者于基线 `27badc1` 实跑为 **54/54 OK**。该脚本只证行号命中，**不证改法正确**。

---

## 输出要求

按仓库 `AGENTS.md` 的评审口径给出 **ACCEPT** 或 **REWORK**，逐条列出问题并附文件路径
与行号。七个问题必须逐一正面回答。若判 REWORK，请明确区分阻塞项与观察项。

约束：只读；不要改任何文件；不要实现；结论写在你的回复里。
