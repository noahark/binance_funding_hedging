[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["backend/tests"]

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["backend"]

[tool.mypy]
python_version = "3.11"
files = ["backend"]
strict = true