# Fable5 对观察项 `O4` 的澄清 — 原始输出（逐字保存）

- 来源: Human 转交的 Fable5 澄清（provider `anthropic`）
- 触发: `22-bookkeeper-design-verification.md` §13.3 要求"写批次 A dispatch 时若该条口径
  仍不明确，应先向 Fable5 索取一句澄清，而不是由 Bookkeeper 代为发挥"
- 落盘者: Bookkeeper `opus5`
- 处置: 逐字保存，未摘要、未改写

```text
先更正一处编号：您描述的是我清单里的 O4（O3 是 1.5 KB 字节口径问题）。以下只澄清 O4 原意，不重新评审。

原意：区分的判据不在载体（代码还是文档），而在更正是否改变任何下游读者会据以行动的东西。纯勘误改完之后，交付物的行为、契约含义、验收判定、评审结论应当与改前完全一致——只是记录更准了；一旦更正使某项检查由不通过变通过、使某条规则语义变化、或实质回应了评审发现，它就是修复，必须计数。否则纯文档交付（如 Harness 阶段）可以把任何返工都包装成"勘误"逃过计数。

可直接写入批次 A 实现任务包的表述（两句）：
▎ 产物勘误仅限不改变交付效果的编辑性更正：只修正文字、格式、引用路径或证据标注，且更正后交付物的代码行为、契约语义、验收标准、各项检查的通过状态与评审结论均须与更正前一致。凡为响应评审发现或 Bookkeeper 拒收而改动上述任何一项的再交付，无论载体是代码还是文档，一律按修复任务递增 rework_count。
```

## Bookkeeper 落盘备注（不属于原始输出）

1. **本次传输未见损坏。** 换行完整，无损坏字符。这是四次评审回执之后的第一份完好
   传输，登记为 `O5`（复议触发线）的对照样本。
2. **编号更正已被接受**，`22-bookkeeper-design-verification.md` §14 逐条落实并声明
   §13.3 的编号被取代。
3. 上方"两句"表述被 Bookkeeper 采纳为**权威原文**，将原样写入批次 A 实现 dispatch，
   不作改写、不作压缩。
