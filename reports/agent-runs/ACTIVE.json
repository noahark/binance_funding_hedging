{
  "active": "2026-07-hedge-open-live-hardening-v1",
  "last_completed": "2026-07-hedge-open-real-api-v1",
  "note": "Stage 2026-07-hedge-open-live-hardening-v1 is IMPLEMENTING on branch stage/2026-07-hedge-open-live-hardening-v1 (from main 4ce9686). MEDIUM, direction panel skipped with user approval. Design/ADR/breakdown archived (Claude Fable 5); parallel mode ON, two tasks, embedded pre-review off; dispatch-ready gate PASSED. Scope: S1 (P0) clientOrderId -> hg{attempt_id}s|p, 35 chars, terminal 38-char rows not migrated; S2 (P1) called as a pure frontend button-condition defect, backend unchanged; S3 (P2) POST /api/hedge-open-settings/start-gate with confirm literal + version CAS + same-transaction audit row; S4 display worker_active/last_worker_exit_reason plus a tri-state leg-existence probe that only refuses when the read succeeded; S5 standalone wire_constraints.py validator on the offline path only. ROUTING as of 2026-07-27 18:55: Kimi quota never recovered, so BOTH implementation tasks are owned by claude_glm and both packets (13 backend, 14 frontend) are running in two separate terminals. Review-1 goes to GROK for BOTH tasks in two separate fresh read-only sessions — explicitly user-enabled, which AGENTS.md requires; evidence 15-user-authorized-grok-review-1.md. Grok model resolved to grok-4.5 (`grok models` reports it as the only available model); command `grok --cwd <repo> --model grok-4.5 --permission-mode plan --prompt-file <file>`. NOTE: agents/registry.yaml adapters.grok is stale — it still pins the now-nonexistent grok-build / grok-composer-2.5-fast in all four command forms; filed as a main-side harness follow-up (status.json.harness_followups) rather than patched inside this stage branch. Pre-authorized fallback if a Grok verdict fails the schema twice: Claude Opus 4.8 for that gate. Review-2 stays codex with zero prior involvement. Both implementation sessions share one working tree, so cross-side test failures may be transient — the bookkeeper's merged-state rerun is the authoritative verdict. Live surface stays CLOSED: service stopped, start_gate=0.",
  "updated_at": "2026-07-27T19:05:00+08:00"
}
