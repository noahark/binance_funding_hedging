# 27-review-2（第三次复审 · 合并决定轮）：2026-07-31-hedge-task-inline-log-v1

> `AGENTS.md` §8：review-2 查**需求、实际效果、证据、运营风险、发布就绪**。
> 本终端**只读**，锚定固定区间，不移动 `HEAD`、不看工作树。

## Identity

- task_id: 2026-07-31-hedge-task-inline-log-v1-review-2-fix2
- target_role: Reviewer（review-2 终审，只读）
- target_model: `codex`
- provider: `openai`
- status_revision: 18
- required_skill: `agents/skills/reality-checker.md`
- rework_count: 2（**上限 3，仅剩一次**；评审轮不递增）

### 隔离状态

你与实现作者 `claude_glm`（zhipu_glm）、review-1 及计划评审 r1-r3 `grok`（xai）、
计划评审 r4 `deepseek`、bookkeeper `opus5`（anthropic）均不同 provider，未参与任何设计
或实现。无需披露。

## 这是第三次由你复审，也是决定能否合并的一轮

你此前两轮的判断与处理结果：

| 轮次 | 你的发现 | 现状 |
|---|---|---|
| 首轮 | **R2-F1**：「成交时间」列展示的是尝试创建时间 | 已修：列头改「尝试时间」+ 去门控。**你上一轮已确认闭合** |
| 首轮 | 发布就绪 fail：合约均价缺失，无法完整判断成本 | 已修：`avg_price` 列 + 两条写路径落库 + 优先展示交易所值。**你上一轮已确认主路径有效** |
| 复审 | **R2-Rerun-F1**：`"0.00000"` 被当成真实均价显示 | 已修（本轮受审）：解析层改数值判零 + 展示层纵深防御 |
| 复审 | O1（`None` 覆盖已知价） | **你已裁定当前不可达、不阻塞合并** |
| 复审 | 发布就绪 fail | 待你本轮重新判断 |

两条阻塞项均已修复，各经一轮 review-1 `ACCEPT`（第二轮 review-1 覆盖了本次解析层改动，
含成交额零语义未被波及的独立验证）。

## Goal

1. **R2-Rerun-F1 是否真的闭合**：
   - 币安返回 `avgPrice="0.00000"` 的已受理未成交订单，现在日志表显示的是 `—` 而不是
     `0`吗？
   - 零值归一改在解析层 + 展示层两处，是否存在仍会漏出 `0` 的路径？
   - **成交额的 `"0"`（真实零成交）有没有被误伤**？这两个零含义相反，是本次修复最容易
     改错的地方。
2. **发布就绪：能否翻转？** 你前两轮均判 fail。请给出**明确结论**：
   - 现在这个功能能否达到「用户展开日志、判断钱的去向」这个目的？
   - 合约均价的可见性够不够（它要等订单详情查询返回才出现，在此之前是 `—`）？
   - 还差什么？如果仍判 fail，请说明**缺的是什么、要做到什么程度才算够**。
3. **合并前 Human 必须知道什么**。你上一轮已提出一条，请确认是否仍需带上，并补充其余：
   > 均价要等订单详情查询返回才会出现；在此之前显示 `—` 是「尚未知」，不是零成本、
   > 也不是没下单。
   另：你上一轮指出交易所均价是**成交均价、不含手续费**，能说明成交价格但不等于最终
   总成本 —— 这条是否也要写进合并说明。
4. **累积风险**：本交付历经 4 轮计划评审 + 3 轮 review-1 + 3 轮 review-2 + 2 轮修复，
   区间 `42de1aff..e9ba1355` 含前后三次交付。请判断**整体**是否仍自洽——有没有哪一轮
   的修复破坏了更早一轮确认过的行为。
5. **遗漏**：以独立视角看，还有什么会影响用户判断钱的去向。

### 关于返工额度

`rework_count` 已达 **2/3**。若你判 `REWORK`，请明确区分「**必须现在修的 in-range
阻塞项**」与「可作为后续项记录的观察」——把可延后的东西计入阻塞会耗尽仅剩的一次额度；
但反过来，**真正影响资金判断的问题不要因为额度紧张而放过**。额度耗尽时由 Human 决定
收窄、重设计、接受限制或停止，那是正常路径，不是失败。

## Allowed Files

只读。不修改任何文件、不提交、不建分支。结论以 `[TASK_RESULT v2]` 文本返回给 Human。

## Inputs

- **受审区间（唯一权威）**：
  `base_sha = 42de1aff364e7c979d2fbb5dc56f1dec65287cc7`
  `delivery_sha = e9ba135541959272d3f6c10d789af702a79f61a7`
  用 `git diff 42de1aff..e9ba1355` 审查。**不要**看工作树、不要移动 `HEAD`。
- 你上一轮的 verdict 与 Bookkeeper 核实：同目录 `21-review-2-rerun-verdict.md`（**必读**）。
- fix-2 packet：同目录 `22-fix2.dispatch.md`；实现者自述：`23-fix2-result.md`。
- **Bookkeeper 对 fix-2 的独立核验**（含零值实测表）：同目录
  `24-bookkeeper-verification-fix2.md`（**必读**）。
- review-1（fix-2 后）的 `ACCEPT`：同目录 `26-review-1-fix2-verdict.md`。
- 更早的记录：`10-`、`12-`、`14-`、`17-`、`19-`（首轮核验、两轮 review-1、你的首轮
  verdict、fix-1 核验）。
- 原需求与钱的展示硬约束：同目录 `00-task.md`（`status_revision: 9`）。
- 范围收窄说明：同目录 `06-scope-reduction.md`。
- 跨 stage 风险台账：`PROJECT_STATE.md`。
- 授权文件：`AGENTS.md`、`agents/roles.md` Reviewer 段、`agents/skills/reality-checker.md`。

### 已知且**不在**本次受审范围的事项

- O1（`None` 覆盖已知价）：**你已裁定**不阻塞合并，保留为上游接口变化时的后续保护项。
- 订单重查间隔 1 秒 → 100ms：已按 Human 决定记为 follow-up（`PROJECT_STATE.md`）。
- 「任务卡卡住」全套（F10、暂停→删除、配额收口、持仓聚合排除 `deleted`）：已移出本 stage。
- 若发现的问题属于上述类别，按 §8 **范围三分类**标注为 `pre-existing-*` 并附早于
  `base_sha` 的引入提交引用。

## Acceptance Checks

- 逐条回答 Goal 五项，每项给出明确判断与依据。
- 每条发现按 §8 标注**范围三分类**；`pre-existing-*` 须附引入提交引用。
- 返回 `[TASK_RESULT v2]`，含 `评审结论: ACCEPT | REWORK`、`问题记录`、`修复要求`。
- **`问题记录` / `修复要求` 不要写 `none`**：写 `inline-full-text` 并把发现清单与修复
  要求放在**同一次输出的正文里**。本 stage 至此七轮评审中有四轮出现正文未随回执转交，
  请勿重演。
- 用中文写结论。**Human 会直接读它做合并决定**——不要交原始 diff、JSON 或技术审查过程。
  说清楚：东西做出来实际效果如何、有什么风险、能不能合并、合并前他必须知道什么。

## Stop

- 只读：不改代码、不改 packet、不改 `status.json`、不提交、不移动 `HEAD`。
- 不做实现、不写修复代码、不启动其他终端。
- 不重做 review-1 的代码审查（已 `ACCEPT`），除非你发现它漏判了影响资金判断的问题。
- 不替 Human 做合并、部署、实盘决策。**`ACCEPT` 不等于合并授权**（`AGENTS.md` §9）。
