# 21：review-2（复审）verdict —— **REWORK**（codex / openai，2026-07-31 18:50:35 CST）

受审区间：`42de1aff364e7c979d2fbb5dc56f1dec65287cc7 .. d85a2d3c1953d635dee59c8a1ccfccdbc40ba73b`。

`评审结论: REWORK（返工）`。阻塞项一条：**R2-Rerun-F1**。

## 已闭合的部分（评审者确认）

| 上轮发现 | 本轮判定 |
|---|---|
| R2-F1「成交时间」列 | **已闭合**。表头现为「尝试时间」，所有尝试都有该时间，不再暗示成交时刻。系统仍无真实成交时间，但当前文案诚实；额外加注解不是本轮阻塞项 |
| 权威均价主路径 | **有效**。合约订单详情查询得到非零 `avgPrice` 后会落库，日志优先展示；历史行退回本地计算 |
| 新旧行并存 | **可接受**。两者表达的都是均价，可能有精度差异，但旧数据无法追溯替换，且不会把未知伪造成价格 |
| 迁移与写入 | **通过**。加性字段迁移可重复、既有数据保留；新增四项后端用例独立复跑通过，前端自检通过；确认 Bookkeeper 已独立复跑 1108 项 |
| O1（`None` 覆盖已知价） | **当前不阻塞**。现有订单详情响应规则下均价与成交额一同返回，触发条件不可达；作为上游接口变化时的后续保护项，**不应以它延迟合并** |

评审者另指出一条业务口径：交易所均价是**成交均价、不含手续费**，因此它能说明成交价格，
**不能单独代表含手续费的最终总成本**。

## R2-Rerun-F1（in-range，阻塞）—— `"0.00000"` 会被当成真实均价显示

### 事实

`_avg_price_decimal`（`live_hedge_executor.py:93-97`）的判零条件是
`raw in (None, "", "0", 0)` —— **只识别字面 `"0"`，不识别 `"0.00000"` / `"0.0"`**。

币安 USDⓈ-M「查询订单」的官方示例正是 `status=NEW`、`executedQty=0`、`cumQuote=0`、
**`avgPrice="0.00000"`**。该值会被解析、落库、并在日志表显示为均价 `0.00000`，
而不是未知的 `—`。

### Bookkeeper 独立复现（固定提交上实测）

```
'0'        -> None      ✓
'0.00000'  -> '0.00000' ✗  ← 币安实际返回的格式
'0.0'      -> '0.0'     ✗
0          -> None      ✓
''         -> None      ✓
'50000.12' -> '50000.12' ✓
```

**确认成立。** 这直接违反本 stage 的钱的展示硬约束「未受理/无价时显示 `—`，绝不显示
`0`」——`0` 会被读成"成交价是零"，而事实是"还没成交"。

### 范围分类：`in-range`（Bookkeeper 认同评审者的判定）

解析器本身早于 `base_sha` 存在，但在本轮之前 `avgPrice` **根本没有落库、也不展示**，
该缺陷不可见。**是本轮新增的「均价优先落库并显示」使它第一次成为用户可见的资金展示
错误。** 故按 §8 记为 `in-range`，走修复轮。

## ⚠️ 这是 Bookkeeper 的判断错误

`15-fix.dispatch.md` 的 Part B 硬约束第 2 条由 Bookkeeper 写死：

> **不得改 `_avg_price_decimal`。** 它已把 `"0"` / 缺失映射为 `None`——这正是本 stage
> 「`0` 不是价格」硬约束要的行为。落库的是 `None` 而非 `"0"`，不要"修"它。

并配 AC8「`git diff` 证明 `live_hedge_executor.py` 未改动」。

**错在**：Bookkeeper 读的是代码字面的元组 `(None, "", "0", 0)`，据此断定判零已经完备，
**从未验证币安实际返回的字符串格式**。一个 `.00000` 的差别使整条硬约束失效。

更严重的是，该禁令**主动堵死了实现者发现并修复它的路**——实现者按 packet 要求核对
「`_avg_price_decimal` 未改」并标记为 pass，是完全正确的执行。

同一处判断在本 stage 出现过两次：Bookkeeper 早前还曾据此向 Human 断言「未成交订单
`avgPrice="0"` → `None`，不会显示 0」。该断言同样基于未验证的格式假设，一并更正。

**本轮修复的 AC8 与该禁令一并撤销**，见 `22-fix2.dispatch.md`。

## Bookkeeper 处置

- 封存本 verdict。`current_task.state` 退回修复，不得写 `verified`。
- **`rework_count` 由 1 递增为 2**（§8：in-range 发现的修复轮递增）。**上限为 3，
  仅剩一次**——已向 Human 明确告知。
- 修复路由：本轮修复改动资金字段的解析语义，按评审者要求「重新经过适用的正式评审」。
  由于改的是 `live_hedge_executor.py` 的解析函数（新文件、新契约面），
  **须重跑 review-1 后再回 review-2**。
- 合并前须让 Human 知道（评审者明确要求）：**均价要等订单详情查询返回才会出现；
  在此之前显示 `—` 是"尚未知"，不是零成本、也不是没下单。**
- O1 按评审者裁定不阻塞合并，保留为后续保护项。
