# 48-review-2-codex-task1-r2 —— review-2 复审结果（codex，只读）

- task_id: `review-2-codex-task1-r2`；评审者 `codex`（`openai`），作者 `claude_glm`（`zhipu_glm`），隔离成立；codex 未参与本 stage 方案设计
- 评审区间：`c1cc10e..ef53a02`（重点 `6d6aa7b..ef53a02`）
- **评审结论：`ACCEPT`**
- **发布就绪结论：可合并，但须先完成一件事** —— 见 §3
- `rework_count` 保持 `2 / 3`

## 1. Bookkeeper 核验

回执格式合规，`ACCEPT` 明确，正文随回执同交。抽验其结论：

| 评审主张 | 复验 | 结果 |
|---|---|---|
| `10-design.md` §5 已补列状态标识 / 均价不完整 / 借款去重 / 8 位有效数字 | §5 差异清单现为 9 条，第 6-9 条即该四项，且第 6 条明确记录「状态信息回到『标记』列而非新增列」这一披露 | 成立 |
| `match_status` 已同步进精确键集 | `test_hedge_api.py:67` 含 `"match_status"` | 成立 |
| 后端 1127 / 前端 self-check 130 | Bookkeeper 已两次自行复跑 | 成立 |
| G5 测试覆盖真实失败形状 | 用真实数据库复验：`RSRUSDT forward` 的 `perp_avg` = `0.001246`、`perp_avg_price_incomplete=True`、`perp_qty` = `20000` | 成立 |

### 1.1 评审提出的一处新事实（Bookkeeper 认同，须让 Human 知悉）

> **未知金额腿被排除出均价分母，因此显示的是「已知金额子集均价」，不保证等于完整持仓成本。**

这是对 G5 修法的准确刻画：修掉了「未知被当成零、把均价砍半」，但排除法本身意味着**均价只覆盖有价格数据的那部分成交**。评审判定不阻塞，理由是「均价不完整」徽标与 `title` 已明确提示。

Bookkeeper 认同该判定，并将其作为**用户须知**记入 §4。

## 2. review-2 的逐项结论（摘要，原文见 §5）

| # | 结论 |
|---|---|
| E1 F3 是否修好 | **已修**。账户验证成功时两类错配有明确标签；`no_task` 行的数量、均价、价差率不再画零而显示 `—`。**D7「都显示、标清楚」已达成**（N2 路径的 F4 属已接受限制） |
| E2 形状一致性 | **通过**。四项新增均已列入 §5；状态信息放入既有「标记」列而非恢复独立列，**已披露**。未发现其它未披露偏离 |
| E3 证据可信度 | **可信**。原始输出真实；G5 测试覆盖真实失败形状。已知证据边界：HTTP 级 N2、混合桶均价、完整运行时验证 |
| E4 运行风险 | **可接受但须知悉**。`match_status` 为新键，前后端须一起发布，外部严格校验旧键集的消费者需同步升级；均价为「已知金额子集均价」；8 位有效数字在非交易量级极端值有显示边界风险（不影响后端记账） |
| E5 发布就绪 | **可合并但须先做某事** —— 见 §3 |
| E6 串行链 | Task 2 可基于 `ef53a02` rebase；F4 的修复要求**充分且可执行**；Task 2 不得把当前限制 A 下的 `single_leg_exposure` 当作完整敞口判断 |

## 3. 合并前的强制前置：完整只读真机冒烟检查

评审明确：

> 必须先由 Human 授权并完成**最终交付（`ef53a02`）**的完整只读真机冒烟，覆盖**账户未就绪、统一账户现货余额匹配、快照陈旧性、页面标签及零交易副作用**。
> `44-runtime-observation-task1.md` 仅覆盖了 `6d6aa7b`、executor disabled、零 running 任务与正常账户读，**不等同于 `ef53a02` 的完整发布检查**。

Bookkeeper 已据此编写执行清单：**`49-preflight-smoke-checklist.md`**。

`ACCEPT` 不授权合并、部署或实盘。

## 4. 交付随附的四条用户须知（合并后仍然有效）

1. **均价是「已知金额子集」的均价**。看到「均价不完整」徽标时，该均价只覆盖有价格数据的那部分成交，**不等于完整持仓成本**。
2. **账户私有数据读不到时，可能把未知状态说成「交易所无仓」**（限制 F4）。判别法：页面上同时出现「账户数据未就绪」横幅时，该行匹配状态不可信。**Task 2 必须修**；未修前合并 `main` 须重新交 Human 裁定。
3. **限制 A / B 仍在**：单腿敞口标记漏报部分失衡；`spot_balance` 与 `drift` 读经典现货账户，`drift` 恒为 `False`。**不能把页面展示当作完整的账户风险证明。**
4. **`match_status` 是新增接口键**，前后端须一起发布；若有外部消费者严格校验旧键集，需同步升级。

## 5. 评审原文（逐字保留）

```text
评审正文（inline-full-text）：

评审区间固定为 c1cc10e..ef53a02，重点 6d6aa7b..ef53a02；未移动 HEAD、未修改文件。交付代码、测试与报告范围 git diff --check 通过；区间内一份 Bookkeeper 控制派工单的尾随空格属于范围外上下文，不影响本交付。

E1｜F3已修复。账户已成功验证时，后端新增 match_status，前端明确显示「无任务记录」或「交易所无仓」；no_task 的本地数量、均价和价差率不再画数字零，而显示 —。G5不完整均价另有独立标记。因此D7「都显示、标清楚」已达成；N2账户不可用路径的F4属于Human已接受限制。

E2｜一致性通过。10-design.md §5已补列：状态标识、均价不完整标记、全仓借款去重、8位有效数字收敛。状态信息放入既有「标记」列而非恢复独立列，已披露。未发现其他未披露的本轮形状偏离。F4已在46-中具名记录并转入Task 2。

E3｜证据可信。原始测试输出 reports/agent-runs/2026-07-31-hedge-task-lifecycle-v1/61-merged-positions-test-output.txt 显示后端1127 passed、前端self-check 130 PASS。G5测试覆盖真实失败形状：一腿金额为字面"0"、另一腿为12.46，断言均价为 0.001246而非0.000623，并确认成交数量保留、不完整标记为真。reports/agent-runs/2026-07-31-hedge-task-lifecycle-v1/44-runtime-observation-task1.md 与 reports/agent-runs/2026-07-31-hedge-task-lifecycle-v1/21-merged-positions-implementation.md 记录了真机/真实数据库核对。HTTP级N2、混合桶均价和完整运行时验证仍是已知证据边界。

E4｜运行风险可接受但须知悉。match_status是新增接口键，内部前端和精确键集测试已同步，前后端需一起发布；外部严格校验旧键集的消费者需同步升级。未知金额腿被排除出均价分母，因此显示的是「已知金额子集均价」，不保证等于完整持仓成本；不完整徽标和title已明确提示，避免把未知当零。8位有效数字能保留0.000623等极小价格；极端到非交易量级时，JavaScript Number/toFixed仍有显示边界风险，不影响后端记账数字。F4仍可能在账户读失败时误显示「交易所无仓」，Human已接受并要求Task 2修复。

E5｜发布就绪结论：可合并但须先做某事。必须先由Human授权并完成最终交付的完整只读真机冒烟，覆盖账户未就绪、统一账户现货余额匹配、快照陈旧性、页面标签及零交易副作用。reports/agent-runs/2026-07-31-hedge-task-lifecycle-v1/44-runtime-observation-task1.md 仅覆盖了6d6aa7b、executor disabled、零running任务和正常账户读，不等同于ef53a02的完整发布检查。ACCEPT不授权合并、部署或实盘。

E6｜Task 2可基于ef53a02 rebase。当前交付保留D15成本基和includes_deleted_task，没有改动状态机。F4修复要求充分且可执行：账户不可用时不得输出no_um、前端不得显示「交易所无仓」、补充verified=false/private_account=None与正常no_um测试，并同步穷举表/设计文档。Task 2不得把当前A限制下的single_leg_exposure当作完整敞口判断。

E7｜Human最该知道的三件事：

1. F3已修好，但含未知成交金额的均价仍是部分口径，看到「均价不完整」不能当作完整成本。
2. 账户私有数据读不到时，当前版本可能把未知状态说成「交易所无仓」；Human已接受为Task 2限制，但Task 2必须修，未修前合并main需重新裁定。
3. 测试全绿且已有零副作用的部分真机观察，但完整只读冒烟尚未完成；限制A/B仍存在，不能把页面展示当作完整账户风险证明。

评审结论: ACCEPT（接受）
问题记录: inline-full-text
修复要求: inline-full-text
本地北京时间: 2026-08-01 18:40:52 CST
下一步模型: opus5（Bookkeeper；Human 将本回执交其同步状态）
下一步任务: 核验本ACCEPT并封存review-2；保留F4与完整只读真机冒烟门槛，随后由Human授权并启动发布前验证
```
