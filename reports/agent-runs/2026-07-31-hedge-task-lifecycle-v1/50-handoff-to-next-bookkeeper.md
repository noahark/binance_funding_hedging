# 50-handoff-to-next-bookkeeper —— 给接手本 stage 的 opus5

- stage: `2026-07-31-hedge-task-lifecycle-v1`
- 写于 2026-08-01，作者：本窗口的 opus5（Task 1 全程的 bookkeeper）
- **你读不到上一个对话窗口，本文件是自包含的交接。** 需要细节时按文中路径取，**不要重新推导已核实的事实**。

## 0. 一句话现状

**四项待办里的第 ① 项（合并持仓表）已完整走完流程并合入本地 `main`。剩下 Task 2 与 Task 3，串行，Task 2 是下一步。**

| 项 | 状态 |
|---|---|
| 分支 | `stage/2026-07-31-hedge-task-lifecycle-v1`（`aca78a6`），已与 `main`（`cc0cbee`）同步 |
| Task 1 交付 | `ef53a02`，合并提交 `d597fef`，**已合本地 main、未 push** |
| `status.json` | revision 21，`current_task: null`，`rework_count: 0`（新交付物重新计） |
| 待办 | **Task 2** → **Task 3**（严格串行，见 §2） |

## 1. 本 stage 的全貌（四项待办）

| # | 内容 | 状态 |
|---|---|---|
| ① | 合并持仓表：以交易所真实 UM 持仓为骨架，匹配现货/杠杆资产与任务卡记录 | **已完成并合并** |
| ② | 任务卡卡死修复 + 五种原因自动删除 + `rate_limited` 改退避 | **Task 2，下一步** |
| ③ | 订单重查间隔 1s → 100ms | **Task 3，在 ② 之后** |
| ④ | `resolve_leg_from_query` 的 `COALESCE` 覆盖保护 | 搭车于 Task 2 |

方案权威：`10-design.md`（八个决策点 P1-P8 + 五个 N1-N5）、`11-adr.md`（ADR-001/002/003）、`12-development-breakdown.md`（三个任务的范围/边界/验收）。**这三份已过计划评审（deepseek ACCEPT），不要重写。**

## 2. Task 2 —— 你的下一步

规格：`12-development-breakdown.md` 的 `## Task 2 —— hedge-task-lifecycle-v1`。**外加一项必做**：

> **必须一并修 F4**（详见 `46-` §3.3）：账户读不到时 `match_status` 不得输出 `no_um`、前端不得显示「交易所无仓」；需要三条能失败的测试；同步 `21-` §11.2 与 `10-design.md` §5。
> 若 Task 2 交付时未做，该限制须重新交 Human 裁定。

**串行约束**：Task 2 基于 `ef53a02` 之后的状态（现在 stage 分支已是），Task 3 再基于 Task 2 的 `delivery_sha` rebase —— ①②③ 都改 `store.py` / `service.py`，不可并行。

**Task 3 的一条具名输入**（本轮 review 之外由 Bookkeeper 穷举发现，评审的消费者审计漏了）：`scheduler.py:51-56` 的唤醒切片由 `interval_us` 推导，降到 100ms 会让调度线程唤醒频率上升约 5 倍（有 5ms 下限，live 下 `tick()` 立即返回故无交易所请求）。

## 3. Human 的决策清单（D1-D16，已定，不要重新论证）

| # | 决策 | 出处 |
|---|---|---|
| D1 | 四项一轮全做（① 变大后再次确认） | `02-` |
| D2 | `done` 语义本轮不处理，不新增状态枚举 | `02-` |
| D3 | 计划评审 `deepseek` / 实现 `claude_glm` / review-1 `grok` / review-2 `codex` | `02-` |
| D4 | 跳过实现前的运行时验证 | `02-` |
| D5/D6/D7 | 以真实 UM 持仓为基准、合并成一张表、对不上的都显示并标清楚、只展示不动作 | `02-` |
| D8 | 先做 fake UI（已完成，`63f5007`） | `02-` |
| D9/D10 | fake UI 作设计探针收口，不评审、不单独合并 | `03-` |
| D11→**D13** | 同币双向**移出本轮范围**（将来由开单闸门根治：有持仓时只允许反向平仓单） | `03-` |
| D12 | 方案由 `claude_glm` 出 | `03-` |
| D14 | 合并在**后端**做 | `04-` |
| D15 | 保留被删除任务的成本基（已实现） | `04-` |
| D16 | `rate_limited` 改退避，**其余五种**自动删除；③ 照做 | `40-` §2 |

## 4. 随 Task 1 一起上线的三条已接受限制

**已提升至 `PROJECT_STATE.md`**（stage 目录归档后仍在）。Task 2 的实现者与评审者都要知道它们**不得据以返工**：

- **A** 单腿敞口标记漏报部分失衡（现货 2.0 / 合约 1.0 判为无敞口）
- **B** `spot_balance` / `drift` 读经典现货账户，而对冲现货腿买入统一账户 → `drift` **永不触发**
- **F4** 账户读不到时谎称「交易所无仓」→ **Task 2 必须修**

五要素记录：A/B 在 `22-` §5，F4 在 `46-` §3。

## 5. 两道 Human 明知而跳过的闸门（如实记录，别粉饰）

- **review-2 的发布前置未执行**：它的 ACCEPT 条件是「先对 `ef53a02` 跑完整只读真机冒烟」，Human 授权直接合并。清单 `49-preflight-smoke-checklist.md` 仍可对 `main` 补跑（未验：账户未就绪路径、统一账户余额匹配、快照陈旧性）。
- **bStock 下单路径修复零评审合入 main**（`3dc6756`，合并提交 `e64c9b5`）：它改的是**真实现货单落到哪个交易对**，属 `HIGH_RISK`，无任何评审。普通币种有兜底不受影响、当前无 bStock 任务，**但首个 bStock 任务一建即触发未评审的实盘行为**。已入 `PROJECT_STATE` Live Risks。

## 6. Task 1 的过程档案（需要时按需取，不必通读）

| 文件 | 内容 |
|---|---|
| `21-` | 实现报告（含 §11.2「每列 × 六场景」穷举表） |
| `22-` | Bookkeeper 拒收记录 + §5 限制 A/B 的五要素 |
| `40-` | 计划评审（deepseek ACCEPT）+ §2 D16 |
| `41-` `42-` `46-` `47-` | review-1 四轮（REWORK → ACCEPT → REWORK → ACCEPT） |
| `43-` `48-` | review-2 两轮（REWORK → ACCEPT） |
| `44-` | **首次真机观察**：真实表格 + 数据库核对，挖出「金额记成字面 0 把均价砍半」 |
| `45-` | Bookkeeper 失误记录：派工单更正提交脱离分支 |
| `49-` | 发布前只读冒烟清单（未执行） |

## 7. 本轮抓到的四个缺陷（同一个根因，Task 2 务必警惕）

**F1** 账户没就绪时整张表不显示 → **F2** 盈亏拿不到画 `0` → **F3** 错配行不标注且假成本显示 `0` → **F4** 没查账户却宣称「交易所无仓」。

**根因始终是同一个：展示层断言了它并不知道的事。** 外加 **G5**：币安移除成交金额字段后未知被存成字面 `"0"`，把合约均价砍掉一半、价差率显示 `-100%`。

Task 2 改的是任务状态机与自动删除，同样会产生大量"要不要告诉用户、怎么说"的判断 —— **这个根因大概率会再来一次。**

## 8. 给你的六条操作纪律（都是本轮真栽过跟头换来的）

1. **每次提交后回验分支归属**（`git branch --show-current && git log --oneline -1`）。我有一次派工单更正提交成了游离提交、从未进入 stage 分支，实现者读到旧版执行（`45-`）。派工单交出去前还要比对分支上的内容是不是最终版。
2. **不要采信"我测过了"**。实现者两轮都自称断言可失败；我自己破坏验证过两次，都真会红 —— 但正因为验了，第一轮那个"前端整个没接上"才没再溜过去。
3. **用真实数据验，不要只信 fixture**。把真实库复制到临时目录跑聚合，才发现均价被砍半（`44-`）。
4. **穷举表会把缺陷写成规范**。上一轮按规则做了「每列 × 六场景」穷举，F4 那一格如实记录了实现、也把错误固化成"预期行为"。要求穷举时必须追加一问：**这一格断言了什么事实？我们真的知道它吗？**
5. **评审的发现要自己复验再采信**。本轮四条发现全部成立，但复验让记录硬得多，也纠正过评审的不完整之处（如 `interval_us` 消费者审计漏两处）。
6. **工作区分支会被别的终端切走**。派工单交出去前提醒 Human 确认分支；本轮就因此发生过实现者读不到文件。

## 9. 你的下一步

1. 按 `AGENTS.md` §4 读：`AGENTS.md` → `ACTIVE.json` → `PROJECT_STATE.md` → 本 stage `status.json` → 本文件 → `agents/roles.md` Bookkeeper 段 + §8。
2. 需要 Task 2 规格时读 `12-development-breakdown.md` 的 Task 2 一节 + `46-` §3.3（F4 的要求），**不要重读整个方案**。
3. 跟 Human 确认开工，然后出 Task 2 的实现 packet 交 `claude_glm`；随后 review-1（`grok`）→ review-2（`codex`）。
4. `rework_count` 从 0 起算（Task 2 是新交付物）。
5. **不要自己启动任何终端**（§3 #2）。
