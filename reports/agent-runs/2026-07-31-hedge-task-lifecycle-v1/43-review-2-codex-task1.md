# 43-review-2-codex-task1 —— review-2 结果（codex，只读）

- task_id: `review-2-codex-task1`
- 评审者：`codex`（`openai`）；实现与修复作者 `claude_glm`（`zhipu_glm`），provider 隔离成立；codex 未参与本 stage 方案设计（方案 `claude_glm` 出、`deepseek` 计划评审）
- 评审区间：`c1cc10e..6d6aa7b`
- **评审结论：`REWORK`**；`问题记录: inline-full-text`；`修复要求: inline-full-text`
- 一条 `in-range` 阻塞发现（F3）
- **发布就绪结论：当前不可合并**；修复 F3 后回 review-2；发布前另需 Human 授权的只读运行时冒烟检查
- 按 `AGENTS.md` §8，`rework_count` 由 `1` 递增为 **`2`**（上限 3，**仅剩 1 次**）

## 1. Bookkeeper 核验：F3 成立，证据比评审所列更硬

回执格式合规，`REWORK` 附可执行修复要求，构成有效返工判定。F3 经三处独立复验，**全部成立**：

| # | 核验点 | 证据 | 结果 |
|---|---|---|---|
| 1 | `no_task` 行的成本字段被填为 `"0"` | `domain.py:1403-1421` `_merge_empty_bucket_row`：`spot_avg: "0"`、`perp_avg: "0"`、`position_qty: "0"` | 成立 |
| 2 | 前端无条件把均价渲染为数字 | `index.html:4529-4530`：`formatHedgeDecimal(p.spot_avg)` / `(p.perp_avg)`，无缺失分支 → `no_task` 行显示「现货均价 0 / 合约均价 0」，读作"在 0 价成交"而非"无记录" | 成立 |
| 3 | 真实表无错配标记 | `index.html:4511-4516` 的 `markers` 只有三种：`单腿敞口` / `本地记录与实际不一致` / `含已删除任务记录`。**「无任务记录」与「交易所无持仓」两类文案在 `index.html` 中不存在**（全文搜索无命中） | 成立 |

### 1.1 Bookkeeper 追加：这是一处**未披露的形状偏离**，性质比评审所述更重

评审将其归入 E2「未列出却被删除」。复验后确认更强的事实：

- fake 交付 `63f5007` 的合并表**有独立的「状态」列**（`index.html:4750-4752` 表头），由 `fkRowStatus`（`:4698-4710`）驱动，四种文案：`正常` / `无任务记录（手工单/卡已删）` / `交易所无持仓（可能已强平/手工平）` / `单腿敞口：现货腿已成交，合约腿无持仓`。
- 该状态列在真实实现中**整列消失**。
- 而 `10-design.md` §5 明文声称：「**展示形状（列 / 六场景视觉 / 三分类）不变**」，并规定「**未列即一致**」，其列出的五条差异**均不含状态列的删除**。

即：方案承诺形状不变、逐条列差异，实际删了一整列且未列出。这不只是"少了个标记"，是**声明与交付不符**。

### 1.2 与 Human 决策的直接冲突

**D7 是 Human 的明确决策：「真实持仓与任务记录对不上时，都显示、标清楚」。**

当前交付做到了"都显示"（`no_task` / `no_um` 行确实返回并渲染），**没做到"标清楚"** —— 两类错配都没有任何标识。且 `no_task` 行把不存在的成本显示为 `0`，比不显示更糟：它是一个看起来像真数字的假数字，与 `PROJECT_STATE.md` 记录的 money-zero tripwire（DEC-2026-07-30-001）同族。

Bookkeeper 认同评审的阻塞判定。

## 2. 修复范围与路由

进入 `fix-merged-positions-mismatch-labels-v1`：

| # | 项 | 必须性 |
|---|---|---|
| G1 | 每个合并行给出明确的来源/匹配状态，真实渲染「无任务记录」与「交易所无持仓」两类文案（对齐 fake `fkRowStatus` 的语义，措辞可优化） | 必须 |
| G2 | `no_task` 行的任务成本字段（`spot_avg` / `perp_avg` / 价差率等本地记账列）不得显示为 `0`，须渲染「暂无」/`—` | 必须 |
| G3 | 补两类错配的前端渲染断言（与既有 R1/R2 断言同标准：须能失败） | 必须 |
| G4 | 不得回退：后端全套测试、前端自检全绿、D15、N2、精确键集、`merge_positions` 纯度、禁改区 | 必须 |

**不得写入修复范围**：已接受限制 A / B（`22-` §5）；Human 推后的建议项与 review-1 观察 C-1~C-4（`42-` §2）。

### 2.1 评审路由（§8）

按 `AGENTS.md` §8：「窄范围的 review-2 发现，在修复、重测、新提交后**直接回到 review-2**；若该修复扩大文件、改变契约或增加风险，须**重新通过 review-1**」。

因此：

- 若修复**仅在既有键内改值**（例如 `no_task` 行的 `spot_avg` 由 `"0"` 改为 `null`）+ 前端渲染与断言 → **直接回 review-2（codex）**。
- 若修复**新增接口键**（例如给每行加一个 `match_status` 字段）→ 属契约扩展，**须先重过 review-1（grok），再回 review-2**。

Bookkeeper 倾向后者（后端已同时知道 UM 侧与任务侧是否存在，显式字段比前端靠"全零"反推稳妥；靠全零反推正是本 stage 反复栽跟头的那类歧义）。但由实现者选择并在回执中声明，Bookkeeper 据此路由。**评审轮次不消耗 `rework_count`**，只消耗时间。

## 3. 返工预算提醒（须 Human 知情）

`rework_count` 现为 **2 / 3**。若 Task 1 再出一轮 `REWORK`，按 `AGENTS.md` §8 将触及上限，届时 **Human 必须在四选一中做决定**：缩小范围、重新设计、接受该缺陷为已知限制、或停止本交付。

Bookkeeper 观察：本轮两次 `REWORK`（F1/F2、F3）**同属一类根因** —— 交付在"展示层如实告知用户"这件事上反复打折（降级路径整表不显示、缺失盈亏画 0、错配行不标注且假成本显示为 0）。§8「同根因刹车」规定：连续两轮 `REWORK` 归因同一根因时，下一个修复任务必须是一次**穷举扫描**，枚举该缺陷家族在受审范围内的全部站点。本次修复 dispatch 已据此要求穷举合并表**每一列**在各场景下的显示口径。

## 4. 发布就绪（review-2 的核心结论，供 Human 决策）

评审明确：**当前不可合并。** 且即使 F3 修复后复审 `ACCEPT`，**发布前仍须 Human 单独授权一次只读运行时冒烟检查**，验证：真实服务可用、账户未就绪路径、统一账户现货余额、快照陈旧性、零交易副作用。

理由：本交付**从未在真实服务上运行过**（Human 于 D4 选择跳过运行时验证；`PROJECT_STATE.md` 亦记录既有 inline-log 功能 `[OPEN][RUNTIME-UNVERIFIED]`）。测试通过不等于发布安全。

评审另指出的运行风险（不阻塞，记为观察）：`verified: true` 只代表曾成功获取，旧快照仍可能被当作实时数据，`checked_at` 仅显示时间、无过期闸门；合并为线性处理、无分页。

## 5. 评审原文（逐字保留，未经改写）

> 以下为 `codex` 只读会话返回的完整正文与 `[TASK_RESULT v2]`，按 `agents/roles.md` Shared Rules「Preserve raw evidence」逐字留档。

```text
Review-2 已完成，固定评审区间为 c1cc10e..6d6aa7b。两套测试复跑通过：后端 1126 passed，前端 self-check 全部通过。

inline-full-text 正文：

E1｜D5/D6/D14/D15 核心实现成立：UM 是合并骨架，后端完成合并，已删除任务成本基保留。D7 仅部分成立：no_task 与 no_um 行虽会返回，但真实表没有明确状态标记；no_task 的现货/合约均价还会显示为 0，容易被误认为真实成本。

E2｜fake UI 已列出的五项差异基本落实；但"状态"列及"无任务记录/交易所无仓"标记未列出却被真实实现删除，属于未披露的形状偏离。

E3｜测试输出为可复现原始输出，后端六场景与 R1/R2 渲染断言有效；但未覆盖两类错配的真实渲染、HTTP 级 N2 形状及混合 D15 均价。证据足以证明现有路径通过，不能证明 D7 完整达成。

E4｜合并为线性处理、无新增交易所请求，性能风险可接受但无分页。verified:true 只代表曾成功获取，旧快照仍可能被当作实时；checked_at 仅显示时间、无过期闸门。1000x 币不会假对齐，但当前没有明确提示。限制 A/B 仍会造成错误安心，且已获 Human 接受，不据此返工。

E5｜当前不可合并：须先修复 F3 并回到 Review-2。即使复审 ACCEPT，发布前仍须 Human 单独授权的只读运行时冒烟检查，验证真实服务、账户未就绪、统一账户现货余额、快照陈旧性及零交易副作用。

E6｜Task 2 可基于 6d6aa7b 串行 rebase，D15 会保留自动删除后的成本基；但限制 A 在任务卡进入已删除筛选后影响显著上升，Task 2 不得把当前 single_leg_exposure 当作完整敞口判断。

E7｜Human 最应知道：① 当前错配行可能不清楚且部分成本显示为 0；② A/B 已接受，但 Task 2 会放大 A 的可见性风险；③ 本交付从未在真实服务运行，不能把测试通过等同于发布安全。

问题记录：

F3 [in-range][blocker]：真实合并表未清楚标记 no_task / no_um 两类错配，且无任务记录的成本列显示 "0"。证据：domain.py:1403-1421 设置零占位；index.html:4511-4516 仅渲染单腿、漂移、已删任务标记；index.html:4529-4530 无条件渲染均价；fake UI 在 index.html:4751-4812 明确有状态列和两类文案。

修复要求：

1. 为每个合并行提供明确的来源/匹配状态，真实渲染"无任务记录"或"交易所无仓"。
2. no_task 的任务成本字段渲染为 —/暂无，不得显示为真实 0。
3. 增加两类错配的前端 self-check；保持现有后端全套测试、前端全绿、D15、N2、精确键集和禁改区不回退。
4. 生成新 commit 后返回 Review-2；若扩展接口契约，按规则先补 Review-1。

发布就绪结论：当前不可合并；修复 F3 后复审，发布前另需只读运行时验证。

[TASK_RESULT v2]
任务 ID: review-2-codex-task1
执行结果: completed（完成）
结果摘要: Review-2 发现 in-range F3：错配行未清楚标记且无任务成本显示0。当前不可合并；修复后回 Review-2，发布前另需只读运行时验证。
产物: [inline-full-text]
检查结果: [pass固定区间与只读隔离; fail E1 D7; fail E2形状偏离; pass E3测试可复现但覆盖缺口见正文; fail E4运行风险; fail E5发布就绪度; pass E6串行rebase可行但限制A风险上升; pass E7]
阻塞项: [F3；修复后新commit回Review-2；发布前Human授权只读运行时冒烟检查]
评审结论: REWORK（返工）
问题记录: inline-full-text
修复要求: inline-full-text
本地北京时间: 2026-08-01 14:05:54 CST
下一步模型: opus5（Bookkeeper；Human 将本回执交其同步状态）
下一步任务: 核验 inline-full-text 的 F3，保持交付未封存，递增返工轮并准备修复 dispatch；Human 启动修复后，以新 commit 重跑检查并回到 Review-2
```
